{
  "name": "bot-faccoes-fivem",
  "version": "1.0.0",
  "description": "Bot profissional de facções para Discord GTA RP/FiveM com registro, farm, ações, vendas, compras, financeiro, rankings, logs e configuração.",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "node index.js",
    "deploy": "node src/deploy-commands.js",
    "check": "node --check index.js && node --check src/index.js && node --check src/deploy-commands.js && node --check src/database.js && node --check src/commands.js && node --check src/ui.js && node --check src/services.js"
  },
  "dependencies": {
    "better-sqlite3": "^11.10.0",
    "discord.js": "^14.19.3",
    "dotenv": "^16.5.0"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=20.11.0"
  }
}
